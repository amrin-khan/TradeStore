# pytest.ini
[pytest]
markers =
    unit: fast, isolated tests
    integration: real services (Kafka/DB)
    regression: end-to-end business flows
addopts = -m "unit and not integration and not regression"
